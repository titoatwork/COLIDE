# =============================================================================
# COLIDE Python Dependencies
# Install: pip install -r requirements.txt
# Minimal CPU/protocol subset: pip install -r requirements-core.txt
# =============================================================================

# Core ML
torch>=2.5.0
numpy>=2.0.0
scipy>=1.13.0

# Model training
scikit-learn>=1.5.0
imbalanced-learn>=0.12.0
joblib>=1.4.0
pyyaml>=6.0
optuna>=3.6.0

# Data / classical teachers / plots
pandas>=2.0.0
matplotlib>=3.8.0
xgboost>=2.0.0
lightgbm>=4.0.0

# GPU power sampling (energy benchmarks; optional at runtime)
pynvml>=11.5.0

# Inference frameworks
onnx>=1.16.0
onnxruntime-gpu>=1.18.0

# LLM explainability
transformers>=4.40.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# Tests (dev)
pytest>=8.0.0
